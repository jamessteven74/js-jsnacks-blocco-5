// Scrivi una funzione che accetti una stringa come argomento e la ritorni girata (es. Ciao -> oaiC)

// creo la parola ciao

const parola = "ciao";

parola.sort(function(ciao) {
    return ciao;
  });

  console.log(parola);
